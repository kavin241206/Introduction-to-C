 #include <stdio.h>
 int main()
 {
     int x, y;
     int length = x;
     int width = y;
     int area;
     int perimeter;
     area = x*y;
     perimeter = 2*(x+y);
     printf("enter the two numbers");
     scanf("%d%d", &x, &y);
     printf("the area is %d\n", x*y);
     printf("the perimeter is %d", 2*(x+y));
     return 0;
 }
