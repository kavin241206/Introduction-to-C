#include <stdio.h>
int main()
{
    float F, C;
    float fahrenheit = F;
    float celcius = C;
    F = (1.8*C)+32;
    printf("enter the value in celcius");
    scanf("%f", &C);
    printf("the value in fahrenheit is %f", F = (1.8*C)+32);
    return 0;
}
