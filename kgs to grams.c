#include <stdio.h>
int main()
{
    float x;
    float kgs = x;
    float grams;
    kgs = 1000*x;
    printf("enter the kgs");
    scanf("%f", &x);
    printf("the grams of the kgs is %f", kgs = 1000*x);
    return 0;
}
