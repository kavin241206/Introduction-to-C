#include <stdio.h>
int main()
{
    float P, C, M;
    float first_subject = P;
    float second_subject = C;
    float third_subject = M;
    float average;
    average = (P+C+M)/3;
    printf("enter the marks of three subjects");
    scanf("%f%f%f", &P, &C, &M);
    printf("the average of the three subjects is %.2f", average = (P+C+M)/3);
    return 0;
}
