#include <stdio.h>
int main()
{
    float R;
    float pi = 22/7, area;
    area = pi*R*R;
    printf("enter the two numbers");
    scanf("%f", &R);
    printf("the area is %f", pi*R*R);
    return 0;
}
