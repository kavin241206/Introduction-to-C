#include <stdio.h>
int main()
{
    float H, L;
    float area;
    area = H*L/2;
    printf("enter the height and lenght");
    scanf("%f%f", &H, &L);
    printf("the area is %f", H*L/2);
    return 0;
}
