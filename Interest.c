#include <stdio.h>
int main()
{
    float I, P, R, N;
    float interest = I;
    float principle_amount = P;
    float Rate = R;
    float Number_of_years = N;
    I = P*R*N/100;
    printf("enter the values");
    scanf("%f%f%f", &P, &R, &N);
    printf("the interest is %f", I= P*R*N/100);
    return 0;
}
