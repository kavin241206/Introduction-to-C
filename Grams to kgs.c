#include <stdio.h>
int main()
{
    float x;
    float grams = x;
    float kgs;
    kgs = x/1000;
    printf("enter the grams");
    scanf("%f", &x);
    printf("the kgs of the given grams are %f", kgs = x/1000);
    return 0;
}
